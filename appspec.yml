version: 0.0
os: linux
files:
-  source: /
   destination: /var/www/weatherapp
hooks:
BeforeInstall:
   - location: bin/clean
     timeout: 10
     runas: root
AfterInstall:
   - location: ./bin/app-start
     timeout: 1200
     runas: root